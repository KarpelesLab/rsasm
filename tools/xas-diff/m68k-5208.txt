# Every form of GNU's m68k opcode table that the ColdFire 5208 (ISA_A+) has and no CPU
# before it in tools/fuzz/m68k.py's CORPUS_ORDER does, one line each, with
# operands that form takes and no earlier form of the mnemonic does.
# GNU syntax.
#
#   tools/fuzz/m68k.py corpus --first --cpu 5208 --syntax gas

strldsr	#-32768
