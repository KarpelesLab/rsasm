; 16-bit real-mode forms, the bulk of what bootloaders are written in.
mov ax, bx
mov ax, 0x1234
mov al, 0x12
mov [bx], ax
mov ax, [bx]
mov ax, [bx+si]
mov ax, [bx+di+2]
mov ax, [si]
mov ax, [di]
mov ax, [bp+2]
mov ax, [bp+si]
mov byte [bx], 1
mov word [bx], 1
mov [es:bx], ax
mov ax, [es:bx]
mov cx, [0x100]
mov [0x100], cx
push ax
push bx
pop cx
push es
pop es
push cs
pusha
popa
pushf
popf
add ax, 1
add ax, 0x100
sub bx, cx
cmp ax, 10
inc ax
dec cx
inc byte [bx]
mul cx
imul ax, bx
shl ax, 1
shr ax, cl
xchg ax, bx
xor ax, ax
test ax, ax
neg ax
not bx
lea ax, [bx+2]
lea si, [bx+si]
cbw
cwd
lodsb
lodsw
stosb
stosw
movsb
movsw
scasb
cmpsb
rep movsb
rep movsw
rep stosb
cld
std
cli
sti
hlt
nop
int 0x10
int 0x21
int3
iret
ret
retf
ret 4
in al, dx
in al, 0x60
out dx, al
out 0x80, al
jmp ax
jmp [bx]
jmp bx
call ax
call [bx]
loop $
jcxz $
jmp $
lgdt [0x100]
lidt [0x100]
lodsb
xlatb
lahf
sahf
